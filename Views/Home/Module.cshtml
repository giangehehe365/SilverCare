@{ var title = ViewData["Title"]?.ToString(); var description = ViewData["Description"]?.ToString(); }
<div class="page-heading"><div><p class="eyebrow">SILVERCARE</p><h1>@title</h1><p class="muted">@description</p></div><button>+ Thêm mới</button></div>
<section class="panel empty-state"><div class="empty-icon">✓</div><h2>@title</h2><p>Đây là màn hình khởi đầu. Bạn có thể bổ sung Model, dữ liệu và các thao tác quản lý ở đây.</p></section>
